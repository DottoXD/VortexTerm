name: vortex_term
description: "An open source, multiplatform full-featured SSH client built with simplicity in mind."
publish_to: 'none'

version: 0.0.1

environment:
  sdk: 3.12.0
  flutter: 3.44.0

dependencies:
  flutter:
    sdk: flutter

  forui: 0.22.3
  shared_preferences: 2.5.5
  xterm: 4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true